node_modules/
.next/
next-env.d.ts
.vercel
*.tsbuildinfo

# Local secrets: DATABASE_URL
.env
.env.local

# Generated by scripts/copy-taxonomy.mjs at predev/prebuild from the committed
# config/carvana-taxonomy.json. Vercel clones the whole repo before cd-ing into this root
# directory, so the source is reachable at build time and the mirror never needs committing.
public/carvana-taxonomy.json
